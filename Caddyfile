# Staging/local mode: listen on plain HTTP to avoid ACME validation failures
# when this host is behind another proxy or only resolves via local DNS.
:80 {
handle /tools/whoami* {
reverse_proxy whoami:80
}

handle {
reverse_proxy invoiceapp:8080
}
}
